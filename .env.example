# TaskPilot currently requires no environment variables.
#
# Future public client configuration may use VITE_ variables, but VITE_ values
# are embedded into browser assets and must never contain secrets.
