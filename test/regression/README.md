# Regression Tests

Add a focused test here whenever a defect escapes into a published build. Include the original symptom and expected behavior in the test name or an adjacent comment.

Current regression coverage also exists in `unit/` for legacy migrations and objective completion.
